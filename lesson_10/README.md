@"
# Домашнее задание №10: Allure и документация

## Описание
В этом проекте реализованы автотесты для калькулятора с использованием:
- **Python** — язык программирования.
- **Selenium** — для автоматизации браузера.
- **Pytest** — для запуска тестов.
- **Allure** — для генерации отчётов.

## Установка зависимостей
Для работы проекта необходимо установить зависимости:
`pip install -r requirements.txt`

## Запуск тестов
Для запуска тестов и генерации Allure-результатов выполните команду:
`pytest lesson_10/ --alluredir=allure-results`

## Просмотр отчёта Allure
Чтобы открыть Allure-отчёт в браузере, выполните:
`allure serve allure-results`

## Дополнительная информация
- Все методы классов задокументированы с указанием типов параметров и возвращаемых значений.
- Код проверен с помощью `flake8`.
"@ | Out-File -FilePath lesson_10\README.md -Encoding utf8
